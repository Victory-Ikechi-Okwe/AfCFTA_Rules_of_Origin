rookie (src/main.rs): a web socket listener that offers the original REST API
rt-proc (src/bin/rt-proc.rs): given arg (doc) runs XT process, otherwise watches a dir for docs
rr-proc (src/bin/rr-proc.rs): IPFS rule synchro